# Flask core
Flask==3.0.0
Flask-SQLAlchemy==3.1.1
Flask-Login==0.6.3
Flask-Migrate==4.0.5
Flask-WTF==1.2.1
WTForms==3.1.1
email-validator==2.1.0

# Database
psycopg2-binary==2.9.9
pgvector==0.2.4

# Cache
redis==5.0.1
cachetools==5.3.2

# Security
bcrypt==4.1.2
python-dotenv==1.0.0

# PDF Processing
PyPDF2==3.0.1
pdfplumber==0.11.0

# YouTube Processing
# IMPORTANT: yt-dlp must be kept up-to-date because YouTube changes their player frequently.
# If transcription stops working, update to the latest version.
youtube-transcript-api==0.6.2
pytubefix==10.3.5
yt-dlp>=2026.3.17
# PO Token provider plugin: required for datacenter IPs to bypass YouTube bot detection.
# Talks to the bgutil-pot sidecar service (see docker-compose.yml).
bgutil-ytdlp-pot-provider>=1.0.0

# AI and ML
openai==1.54.0
groq==0.11.0
faster-whisper==1.1.0
sentence-transformers==2.3.1
transformers==4.36.2
# torch is installed separately in the Dockerfile from the CPU-only index
# to avoid pulling ~3GB of CUDA libraries
numpy<2.0.0
tiktoken==0.5.2

# HTTP requests
requests==2.31.0
httpx<0.28.0  # Pin to version compatible with openai==1.54.0

# Utilities
python-dateutil==2.8.2
Werkzeug==3.0.1

# Testing (optional)
pytest==7.4.3
pytest-flask==1.3.0
